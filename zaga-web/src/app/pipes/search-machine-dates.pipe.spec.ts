import { SearchMachineDatesPipe } from './search-machine-dates.pipe';

describe('SearchMachineDatesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMachineDatesPipe();
    expect(pipe).toBeTruthy();
  });
});
