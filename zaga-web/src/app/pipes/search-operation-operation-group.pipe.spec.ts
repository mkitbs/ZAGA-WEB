import { SearchOperationOperationGroupPipe } from './search-operation-operation-group.pipe';

describe('SearchOperationOperationGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchOperationOperationGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
