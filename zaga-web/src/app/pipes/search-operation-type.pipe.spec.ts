import { SearchOperationTypePipe } from './search-operation-type.pipe';

describe('SearchOperationTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchOperationTypePipe();
    expect(pipe).toBeTruthy();
  });
});
