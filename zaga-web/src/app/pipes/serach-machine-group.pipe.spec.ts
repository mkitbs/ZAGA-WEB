import { SerachMachineGroupPipe } from './serach-machine-group.pipe';

describe('SerachMachineGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new SerachMachineGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
