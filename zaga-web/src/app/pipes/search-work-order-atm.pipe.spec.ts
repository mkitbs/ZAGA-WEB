import { SearchWorkOrderAtmPipe } from './search-work-order-atm.pipe';

describe('SearchWorkOrderAtmPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchWorkOrderAtmPipe();
    expect(pipe).toBeTruthy();
  });
});
