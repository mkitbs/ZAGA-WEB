import { SearchVarietyPipe } from './search-variety.pipe';

describe('SearchVarietyPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchVarietyPipe();
    expect(pipe).toBeTruthy();
  });
});
