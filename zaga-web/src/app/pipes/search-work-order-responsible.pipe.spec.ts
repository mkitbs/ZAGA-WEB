import { SearchWorkOrderResponsiblePipe } from './search-work-order-responsible.pipe';

describe('SearchWorkOrderResponsiblePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchWorkOrderResponsiblePipe();
    expect(pipe).toBeTruthy();
  });
});
