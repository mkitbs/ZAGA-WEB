import { SearchMachineSapIdPipe } from './search-machine-sap-id.pipe';

describe('SearchMachineSapIdPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMachineSapIdPipe();
    expect(pipe).toBeTruthy();
  });
});
