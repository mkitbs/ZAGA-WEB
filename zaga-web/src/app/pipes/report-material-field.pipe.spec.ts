import { ReportMaterialFieldPipe } from './report-material-field.pipe';

describe('ReportMaterialFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new ReportMaterialFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
