import { ReportMaterialCropPipe } from './report-material-crop.pipe';

describe('ReportMaterialCropPipe', () => {
  it('create an instance', () => {
    const pipe = new ReportMaterialCropPipe();
    expect(pipe).toBeTruthy();
  });
});
