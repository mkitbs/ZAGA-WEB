import { ReportMaterialOperationPipe } from './report-material-operation.pipe';

describe('ReportMaterialOperationPipe', () => {
  it('create an instance', () => {
    const pipe = new ReportMaterialOperationPipe();
    expect(pipe).toBeTruthy();
  });
});
