import { SearchWorkOrderCropPipe } from './search-work-order-crop.pipe';

describe('SearchWorkOrderCropPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchWorkOrderCropPipe();
    expect(pipe).toBeTruthy();
  });
});
