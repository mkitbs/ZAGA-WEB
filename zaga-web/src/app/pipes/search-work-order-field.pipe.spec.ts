import { SearchWorkOrderFieldPipe } from './search-work-order-field.pipe';

describe('SearchWorkOrderFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchWorkOrderFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
