import { SearchWorkOrderDayPipe } from './search-work-order-day.pipe';

describe('SearchWorkOrderDayPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchWorkOrderDayPipe();
    expect(pipe).toBeTruthy();
  });
});
