import { SearchCouplingMachinesPipe } from './search-coupling-machines.pipe';

describe('SearchCouplingMachinesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCouplingMachinesPipe();
    expect(pipe).toBeTruthy();
  });
});
