import { ReportMaterialMaterialPipe } from './report-material-material.pipe';

describe('ReportMaterialMaterialPipe', () => {
  it('create an instance', () => {
    const pipe = new ReportMaterialMaterialPipe();
    expect(pipe).toBeTruthy();
  });
});
