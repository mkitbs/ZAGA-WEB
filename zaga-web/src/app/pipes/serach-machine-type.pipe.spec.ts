import { SerachMachineTypePipe } from './serach-machine-type.pipe';

describe('SerachMachineTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SerachMachineTypePipe();
    expect(pipe).toBeTruthy();
  });
});
