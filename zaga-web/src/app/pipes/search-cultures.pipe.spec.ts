import { SearchCulturesPipe } from './search-cultures.pipe';

describe('SearchCulturesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCulturesPipe();
    expect(pipe).toBeTruthy();
  });
});
