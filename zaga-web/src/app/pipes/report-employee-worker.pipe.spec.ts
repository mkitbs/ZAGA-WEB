import { ReportEmployeeWorkerPipe } from './report-employee-worker.pipe';

describe('ReportEmployeeWorkerPipe', () => {
  it('create an instance', () => {
    const pipe = new ReportEmployeeWorkerPipe();
    expect(pipe).toBeTruthy();
  });
});
