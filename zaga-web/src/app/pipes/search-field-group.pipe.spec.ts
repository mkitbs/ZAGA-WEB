import { SearchFieldGroupPipe } from './search-field-group.pipe';

describe('SearchFieldGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFieldGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
