import { SearchWorkOrdersOperationPipe } from './search-work-orders-operation.pipe';

describe('SearchWorkOrdersOperationPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchWorkOrdersOperationPipe();
    expect(pipe).toBeTruthy();
  });
});
