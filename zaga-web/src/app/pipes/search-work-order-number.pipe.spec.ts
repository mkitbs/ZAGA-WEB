import { SearchWorkOrderNumberPipe } from './search-work-order-number.pipe';

describe('SearchWorkOrderNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchWorkOrderNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
