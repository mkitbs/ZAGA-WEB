import { SearchCulturePipe } from './search-culture.pipe';

describe('SearchCulturePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCulturePipe();
    expect(pipe).toBeTruthy();
  });
});
