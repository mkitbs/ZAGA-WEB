import { SearchVarietyCultureGroupPipe } from './search-variety-culture-group.pipe';

describe('SearchVarietyCultureGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchVarietyCultureGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
