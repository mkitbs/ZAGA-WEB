import { SearchCultureGroupPipe } from './search-culture-group.pipe';

describe('SearchCultureGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCultureGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
