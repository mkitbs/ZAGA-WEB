import { SearchWorkOrdersDatePipe } from './search-work-orders-date.pipe';

describe('SearchWorkOrdersDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchWorkOrdersDatePipe();
    expect(pipe).toBeTruthy();
  });
});
