import { SearchOperationsPipe } from './search-operations.pipe';

describe('SearchOperationsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchOperationsPipe();
    expect(pipe).toBeTruthy();
  });
});
