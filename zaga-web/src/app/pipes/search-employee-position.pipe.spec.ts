import { SearchEmployeePositionPipe } from './search-employee-position.pipe';

describe('SearchEmployeePositionPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchEmployeePositionPipe();
    expect(pipe).toBeTruthy();
  });
});
