import { SearchMachineMachineGroupPipe } from './search-machine-machine-group.pipe';

describe('SearchMachineMachineGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMachineMachineGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
