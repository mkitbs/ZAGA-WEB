import { SearchPropulsionMachinesPipe } from './search-propulsion-machines.pipe';

describe('SearchPropulsionMachinesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPropulsionMachinesPipe();
    expect(pipe).toBeTruthy();
  });
});
