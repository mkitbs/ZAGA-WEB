import { SearchMaterialsPipe } from './search-materials.pipe';

describe('SearchMaterialsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMaterialsPipe();
    expect(pipe).toBeTruthy();
  });
});
