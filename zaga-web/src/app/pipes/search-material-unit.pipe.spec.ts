import { SearchMaterialUnitPipe } from './search-material-unit.pipe';

describe('SearchMaterialUnitPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMaterialUnitPipe();
    expect(pipe).toBeTruthy();
  });
});
