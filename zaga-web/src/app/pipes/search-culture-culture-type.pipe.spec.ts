import { SearchCultureCultureTypePipe } from './search-culture-culture-type.pipe';

describe('SearchCultureCultureTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCultureCultureTypePipe();
    expect(pipe).toBeTruthy();
  });
});
