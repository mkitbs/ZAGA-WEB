import { ReportMaterialSapIdPipe } from './report-material-sap-id.pipe';

describe('ReportMaterialSapIdPipe', () => {
  it('create an instance', () => {
    const pipe = new ReportMaterialSapIdPipe();
    expect(pipe).toBeTruthy();
  });
});
