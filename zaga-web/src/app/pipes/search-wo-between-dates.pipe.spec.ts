import { SearchWoBetweenDatesPipe } from './search-wo-between-dates.pipe';

describe('SearchWoBetweenDatesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchWoBetweenDatesPipe();
    expect(pipe).toBeTruthy();
  });
});
