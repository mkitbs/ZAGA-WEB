import { SearchMachinePipe } from './search-machine.pipe';

describe('SearchMachinePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMachinePipe();
    expect(pipe).toBeTruthy();
  });
});
