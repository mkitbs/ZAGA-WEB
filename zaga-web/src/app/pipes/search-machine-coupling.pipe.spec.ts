import { SearchMachineCouplingPipe } from './search-machine-coupling.pipe';

describe('SearchMachineCouplingPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMachineCouplingPipe();
    expect(pipe).toBeTruthy();
  });
});
