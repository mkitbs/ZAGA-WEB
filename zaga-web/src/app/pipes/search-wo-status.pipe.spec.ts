import { SearchWoStatusPipe } from './search-wo-status.pipe';

describe('SearchWoStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchWoStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
