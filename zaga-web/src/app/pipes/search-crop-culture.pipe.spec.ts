import { SearchCropCulturePipe } from './search-crop-culture.pipe';

describe('SearchCropCulturePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCropCulturePipe();
    expect(pipe).toBeTruthy();
  });
});
