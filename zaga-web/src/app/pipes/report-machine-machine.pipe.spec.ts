import { ReportMachineMachinePipe } from './report-machine-machine.pipe';

describe('ReportMachineMachinePipe', () => {
  it('create an instance', () => {
    const pipe = new ReportMachineMachinePipe();
    expect(pipe).toBeTruthy();
  });
});
