import { SearchEmployeesPositionPipe } from './search-employees-position.pipe';

describe('SearchEmployeesPositionPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchEmployeesPositionPipe();
    expect(pipe).toBeTruthy();
  });
});
