import { SearchCultureCultureGroupPipe } from './search-culture-culture-group.pipe';

describe('SearchCultureCultureGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCultureCultureGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
