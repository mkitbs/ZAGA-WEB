import { SearchCultureTypePipe } from './search-culture-type.pipe';

describe('SearchCultureTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCultureTypePipe();
    expect(pipe).toBeTruthy();
  });
});
