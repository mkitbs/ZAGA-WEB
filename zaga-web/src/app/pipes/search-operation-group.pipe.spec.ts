import { SearchOperationGroupPipe } from './search-operation-group.pipe';

describe('SearchOperationGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchOperationGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
