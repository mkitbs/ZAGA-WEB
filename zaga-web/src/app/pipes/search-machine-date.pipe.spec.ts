import { SearchMachineDatePipe } from './search-machine-date.pipe';

describe('SearchMachineDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMachineDatePipe();
    expect(pipe).toBeTruthy();
  });
});
