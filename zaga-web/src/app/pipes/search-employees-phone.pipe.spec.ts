import { SearchEmployeesPhonePipe } from './search-employees-phone.pipe';

describe('SearchEmployeesPhonePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchEmployeesPhonePipe();
    expect(pipe).toBeTruthy();
  });
});
