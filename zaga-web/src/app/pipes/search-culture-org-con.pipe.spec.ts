import { SearchCultureOrgConPipe } from './search-culture-org-con.pipe';

describe('SearchCultureOrgConPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCultureOrgConPipe();
    expect(pipe).toBeTruthy();
  });
});
